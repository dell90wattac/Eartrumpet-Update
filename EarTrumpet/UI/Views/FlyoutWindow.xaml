<Window x:Class="EarTrumpet.UI.Views.FlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Event="clr-namespace:EarTrumpet.Extensions.EventBinding"
        xmlns:Theme="clr-namespace:EarTrumpet.UI.Themes"
        xmlns:b="clr-namespace:EarTrumpet.UI.Behaviors"
        xmlns:ctl="clr-namespace:EarTrumpet.UI.Controls"
        xmlns:resx="clr-namespace:EarTrumpet.Properties"
        xmlns:views="clr-namespace:EarTrumpet.UI.Views"
        xmlns:vm="clr-namespace:EarTrumpet.UI.ViewModels"
        Title="EarTrumpet"
        Width="{StaticResource WindowAndItemSize}"
        Theme:Brush.Foreground="Text"
        Theme:Options.Scope="Flyout"
        Theme:Options.Source="System"
        b:FrameworkElementEx.DisplaySettingsChanged="{Binding DisplaySettingsChanged}"
        b:FrameworkElementEx.FlowDirection="Auto"
        Background="Transparent"
        Closing="{Event:Binding OnClosing}"
        Deactivated="{Event:Binding OnDeactivated}"
        Icon="{Binding Source={StaticResource EarTrumpetIconLight}}"
        PreviewKeyDown="{Event:Binding OnPreviewKeyDown}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        WindowStyle="None"
        FontFamily="{StaticResource AppFontFamily}">
    <Window.Resources>
        <Style x:Key="{x:Type ScrollViewer}"
               BasedOn="{StaticResource {x:Type ScrollViewer}}"
               TargetType="{x:Type ScrollViewer}">
            <Setter Property="CanContentScroll" Value="True" />
            <Setter Property="PanningMode" Value="VerticalOnly" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Margin" Value="0" />
        </Style>
    </Window.Resources>
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Name="BaseVisual" VerticalScrollBarVisibility="Auto">
            <Grid Name="ContentGrid" VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Nothing to show: no endpoints at all.  -->
                <Border Grid.Row="0"
                        Grid.RowSpan="4"
                        VerticalAlignment="Center"
                        Theme:Brush.Background="FlyoutBackground">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Output.HasDevice}" Value="False" />
                                        <Condition Binding="{Binding Input.HasDevice}" Value="False" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Margin="{StaticResource NoItemsPaneMargin}"
                               HorizontalAlignment="Center"
                               FontSize="{StaticResource {x:Static SystemFonts.MessageFontSizeKey}}"
                               Text="{x:Static resx:Resources.NoDevicesPanelContent}"
                               TextWrapping="Wrap" />
                </Border>

                <views:DeviceRowView Grid.Row="0" DataContext="{Binding Output}" />

                <Border Grid.Row="1"
                        Height="1"
                        Margin="12,0,12,0"
                        Theme:Brush.Background="Theme=#14FFFFFF, Light=#0F000000, HighContrast=WindowText"
                        Visibility="{Binding Input.HasDevice, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <views:DeviceRowView Grid.Row="2" DataContext="{Binding Input}" />

                <!--  Apps on the endpoint currently in use for playback.  -->
                <Grid Grid.Row="3" Margin="0,2,0,6">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Output.Default.Apps.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Output.HasDevice}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Height="1"
                            Margin="12,0,12,6"
                            Theme:Brush.Background="Theme=#14FFFFFF, Light=#0F000000, HighContrast=WindowText" />

                    <StackPanel Grid.Row="1">
                        <TextBlock Margin="20,0,20,4"
                                   FontSize="{DynamicResource Mutable_DeviceKindLabelFontSize}"
                                   Theme:Brush.Foreground="GrayText"
                                   Text="{x:Static resx:Resources.AppsHeaderText}" />
                        <ListView Name="AppList"
                                  Margin="{StaticResource VolumeAppListMargin}"
                                  AutomationProperties.Name="{x:Static resx:Resources.AppsListAccessibleText}"
                                  ItemsSource="{Binding Output.Default.Apps, Mode=OneWay}" />
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>
        <Border Grid.Row="0"
                Name="LightDismissBorder"
                Theme:Brush.Background="Background"
                PreviewMouseDown="{Event:Binding OnLightDismissBorderPreviewMouseDown}">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Dialog.IsVisible}" Value="True" />
                                <Condition Binding="{Binding Source={StaticResource ThemeManager}, Path=AnimationsEnabled}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOff" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOff" />
                                <BeginStoryboard Name="EnterLightDismiss_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0"
                                                         To="1"
                                                         Duration="00:00:00.15" />
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOff" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOff" />
                                <BeginStoryboard Name="ExitLightDismiss_AnimationsOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="1"
                                                         To="0"
                                                         Duration="00:00:00.15" />
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00.15" Value="{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Dialog.IsVisible}" Value="True" />
                                <Condition Binding="{Binding Source={StaticResource ThemeManager}, Path=AnimationsEnabled}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOn" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOn" />
                                <BeginStoryboard Name="EnterLightDismiss_AnimationsOff">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="EnterLightDismiss_AnimationsOn" />
                                <StopStoryboard BeginStoryboardName="ExitLightDismiss_AnimationsOn" />
                                <BeginStoryboard Name="ExitLightDismiss_AnimationsOff">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetProperty="(UIElement.Visibility)">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Collapsed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border Margin="0,0,0,14">
                <Border.Effect>
                    <BlurEffect KernelType="Gaussian"
                                Radius="{StaticResource WindowBlurRadius}"
                                RenderingBias="Performance" />
                </Border.Effect>
                <Border.Background>

                    <VisualBrush AlignmentX="Left"
                                 AlignmentY="Top"
                                 Stretch="UniformToFill"
                                 Visual="{Binding ElementName=BaseVisual}" />
                </Border.Background>
            </Border>
        </Border>
        <!--
            The Windows audio panels, one click from the flyout. Each of these
            used to be two levels deep in the tray right-click menu, behind a
            submenu hover.
        -->
        <Border Grid.Row="1"
                Padding="6"
                Theme:Brush.Background="Theme=#0FFFFFFF, Light=#08000000, HighContrast=Window"
                Theme:Brush.BorderBrush="Theme=#18FFFFFF, Light=#14000000, HighContrast=WindowText"
                BorderThickness="0,1,0,0">
            <UniformGrid Columns="5" Rows="1">
                <Button Command="{Binding OpenPlaybackDevices}"
                        Style="{StaticResource QuickActionButtonStyle}"
                        Tag="&#xE995;"
                        Content="{x:Static resx:Resources.QuickActionPlaybackText}" />
                <Button Command="{Binding OpenRecordingDevices}"
                        Style="{StaticResource QuickActionButtonStyle}"
                        Tag="&#xE720;"
                        Content="{x:Static resx:Resources.QuickActionRecordingText}" />
                <Button Command="{Binding OpenLegacyMixer}"
                        Style="{StaticResource QuickActionButtonStyle}"
                        Tag="&#xE9E9;"
                        Content="{x:Static resx:Resources.QuickActionMixerText}" />
                <Button Command="{Binding OpenSoundSettings}"
                        Style="{StaticResource QuickActionButtonStyle}"
                        Tag="&#xE7F3;"
                        Content="{x:Static resx:Resources.QuickActionSoundText}" />
                <Button Command="{Binding OpenSettings}"
                        Style="{StaticResource QuickActionButtonStyle}"
                        Tag="&#xE713;"
                        Content="{x:Static resx:Resources.QuickActionSettingsText}" />
            </UniformGrid>
        </Border>

        <ctl:AppPopup IsOpen="{Binding Dialog.IsVisible, Mode=TwoWay}"
                      PlacementTarget="{Binding Dialog.Source}"
                      Style="{StaticResource AppPopup}">
            <ContentControl Content="{Binding Dialog.Focused}" />
        </ctl:AppPopup>
    </Grid>
</Window>
